apiVersion: v1
kind: Service
metadata:
  name: bid-server-service
  labels:
    app: bid-server
    metrics: dev-monitor
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8082
      targetPort: 8082
      protocol: TCP
  selector:
    app: bid-server