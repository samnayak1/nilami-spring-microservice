apiVersion: v1
kind: Service
metadata:
  name: bid-server-service
  labels:
    app: bid-server
spec:
  type: ClusterIP
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
  selector:
    app: bid-server