apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  labels:
    metrics: dev-monitor
spec:
  type: ClusterIP
  selector:
    app: api-gateway
  ports:
    - name: http
      port: 8084
      targetPort: 8084
