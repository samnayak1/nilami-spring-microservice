apiVersion: v1
kind: Service
metadata:
  name: registry-service 
  labels:
    app: registry-service
    metrics: dev-monitor
spec:
  type: ClusterIP 
  selector:
    app: registry-service
  ports:
    - name: http
      protocol: TCP
      port: 8761      
      targetPort: 8761 